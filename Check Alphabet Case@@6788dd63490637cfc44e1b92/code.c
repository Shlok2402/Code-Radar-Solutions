#include <stdio.h>

int main() {
    char b;
    scanf("%c", &a);
    if (A <= b <= Z){
        printf("Uppercase");
    }
    else if(a <= b <= z){
        printf("Lowercase");
    } 
    return 0;
}